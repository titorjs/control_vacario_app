import 'package:flutter/material.dart';

class DailyProductionScreen extends StatelessWidget {
  const DailyProductionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DailyProductionScreen'),
      ),
      body: const Center(
        child: Text('Aquí irá la pantalla de DailyProductionScreen'),
      ),
    );
  }
}
